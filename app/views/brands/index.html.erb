<h1>Markalar</h1>
<%
=begin%>
This is the main page. All brands is shown as a table here .
<%
=end%>
<%= link_to '<span style="margin-left:110%;margin-top:10px;" class="glyphicon glyphicon-plus" ></span>'.html_safe, new_brand_path%>
<table class="table" style="margin-top: 50px; margin-left:85%;   width:30%;">

    <thead>

        <tr>
            <th style="text-align:center;width:70px;">Marka Adı</th>
            <th>Görüntüle</th>
            <th>Düzenle</th>
            <th>Sil</th>

            <th colspan="2"></th>
        </tr>
    </thead>
    <% @brands.each do |brand| %>
    <tr>
        <td style="width:20%;text-align:center;"><%= brand.title %></td>
        <td style="width:1%;"><%= link_to '<span class="glyphicon glyphicon-eye-open"></span>'.html_safe, brand_path(brand) %></td>
        <%
=begin%>
        Showing the brand
        <%
=end%>
        <td style="width:1%;"> <%= link_to '<span class="glyphicon glyphicon-edit"></span>'.html_safe, edit_brand_path(brand) %></td>
        <%
=begin%>
        We can edit the brand
        <%
=end%>
        <td style="width:1%;"> <%= link_to '<span class="glyphicon glyphicon-trash"></span>'.html_safe,brand_path(brand),
              method: :delete,
              data: { confirm: 'Emin misiniz?' } %></td>
        <%
=begin%>
        We can delete the brand and it's all products too.
        <%
=end%>
    </tr>
    <% end %>
</table>